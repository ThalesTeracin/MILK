[pytest]
testpaths = tests
; Os testes assumem cwd = raiz do repositorio (C:\JARVIS), pois varios
; modulos usam caminhos relativos como Path("config/...").
; Rode sempre com: cd C:\JARVIS && python -m pytest
